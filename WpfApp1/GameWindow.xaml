<Window x:Class="WpfApp1.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Гра" Height="300" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#f0f8ff"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Стиль для кнопок з закругленням -->
        <Style TargetType="Button" x:Key="RoundedButton">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="White" CornerRadius="15" Padding="20" Margin="20"
                BorderBrush="#87cefa" BorderThickness="3">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                <TextBlock x:Name="WordTextBlock"
                           FontSize="28"
                           FontWeight="Bold"
                           TextAlignment="Center"
                           Margin="10"
                           Foreground="#333"/>

                <TextBlock x:Name="TranslationTextBlock"
                           FontSize="22"
                           FontStyle="Italic"
                           TextAlignment="Center"
                           Margin="5"
                           Foreground="#555"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="15">
                    <Button Content="Перевірити" Width="100" Height="35"
                            Style="{StaticResource RoundedButton}" 
                            Click="CheckButton_Click"/>

                    <Button Content="Наступне" Width="100" Height="35"
                            Style="{StaticResource RoundedButton}" 
                            Background="#2196F3"
                            Click="NextButton_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>
    </Grid>
</Window>